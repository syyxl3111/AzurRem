<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      桌面 / 最近任务里显示的名字。

      ⚠️ 这是**客户端**的名字，不是被连的那个服务端。
      服务端仍然叫 AzurPilot —— 数据桥、MCP、日志路径全都不受影响。
      之所以刻意区分开，是因为两边会同时出现在同一屏上（状态卡里那个
      「alas」是**实例名**，是第三个概念，也别混）。
    -->
    <string name="app_name">AzurRem</string>
</resources>
